// context.js
const context = {
    setIsAuthorized: (value) => {
      // Logique pour définir l'autorisation dans le contexte
    },
    // Autres propriétés de contexte
  };
  
  module.exports = context;
  